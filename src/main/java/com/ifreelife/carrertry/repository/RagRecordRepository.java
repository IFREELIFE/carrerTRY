package com.ifreelife.carrertry.repository;

import com.ifreelife.carrertry.entity.RagRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RagRecordRepository extends JpaRepository<RagRecord, Long> {
}
